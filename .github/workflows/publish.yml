name: Publish to npm

on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # Required for npm OIDC
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          # NOTE: Do not set registry-url here; setup-node will otherwise inject GITHUB_TOKEN into npm auth.

      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build

      - name: Publish with provenance
        run: npm publish --provenance --access public
        env:
          NPM_CONFIG_PROVENANCE: true
